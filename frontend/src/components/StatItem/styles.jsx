import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    statItemContainer: {
        flexDirection: "row",
        alignItems: "center",
        marginHorizontal: 10,
        gap: 10
    },
    statItemText: {
        fontSize: 18,
        flexShrink: 1
    }
})

export default styles